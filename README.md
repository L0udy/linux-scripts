# linux-scripts
Collection of useful Linux scripts for system configuration and automation

## Scripts

- `fix_nvidia_suspend.sh`: Fixes suspend/resume black screen on Fedora with NVIDIA GPUs.

## apt-lp (Ubuntu Patch Summary)

A zypper-style patch summary tool for Ubuntu.
 
  Install
  
```bash
curl -O https://raw.githubusercontent.com/L0udy/linux-scripts/main/install-apt-lp.sh
chmod +x install-apt-lp.sh
./install-apt-lp.sh
